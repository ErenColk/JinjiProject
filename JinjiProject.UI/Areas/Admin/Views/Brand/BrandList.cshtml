@model List<ListBrandDto>


<div class="content">
    <div class="container-fluid">
        <h4 class="page-title">MARKA</h4>
        <div class="row">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">Marka İşlemleri</div>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table class="table mt-4">
                                <thead>
                                    <tr>
                                        <th scope="col">#</th>
                                        <th scope="col">Marka Adı</th>
                                        <th scope="col">Durumu</th>
                                        <th scope="col">Güncelle</th>
                                        <th scope="col">Detaylar</th>
                                        <th scope="col">Sil</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @if (Model == null)
                                    {

                                    }
                                    else
                                    {
                                        @foreach (var item in Model)
                                        {
                                            <tr>
                                                <td>@item.Id</td>
                                                <td>@item.Name</td>
                                                <td>@GetEnumDescription.Description(item.Status)</td>
                                                <td>
                                                    <div class="d-grid">
                                                        <a href="/Admin/Brand/UpdateBrand/@item.Id" class="btn btn-outline-success btn-sm">Güncelle</a>
                                                    </div>
                                                </td>
                                                <td>
                                                    <div class="d-grid">
                                                        <button type="button" class="btn btn-outline-danger btn-sm" onclick="loadBrandData(@item.Id)" id="modalOpener" data-bs-toggle="modal" data-bs-target="#exampleModal" data-id="@item.Id">
                                                            Detaylar
                                                        </button>
                                                    </div>
                                                </td>
                                                <td>
                                                    <div class="d-grid">
                                                        <a href="/Admin/Brand/SoftDelete/@item.Id" class="btn btn-outline-danger btn-sm">Sil</a>
                                                    </div>
                                                </td>
                                            </tr>
                                        }
                                    }
                                </tbody>
                            </table>
                        </div>
                        <a href="/Admin/Brand/CreateBrand" class="btn btn-outline-primary">Yeni Marka Girişi </a>
                        <a style="margin-left:10px" href="/Admin/Brand/DeletedBrandList" class="btn btn-outline-danger">Silinen Markalar</a>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>


<partial name="~/Areas/Admin/Views/Brand/_BrandDetailPartialView.cshtml" />
<script src="~/js/detail-brand.js"></script>