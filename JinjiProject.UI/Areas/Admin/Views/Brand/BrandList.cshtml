@model List<ListBrandDto>


<div class="content">
    <div class="container-fluid">
        <h4 class="page-title">MARKA</h4>
        <div class="row">
            <div class="col">
                <div class="card">
                    <div class="card-header">
                        <div class="card-title">Marka İşlemleri</div>
                    </div>
                    <div class="card-body">
                        <table class="table mt-4">
                            <thead>
                                <tr>
                                    <th scope="col">#</th>
                                    <th scope="col">Marka Adı</th>
                                    <th scope="col">Durumu</th>
                                    <th scope="col">Güncelle</th>
                                    <th scope="col">Detaylar</th>
                                    <th scope="col">Sil</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (Model == null)
                                {

                                }
                                else
                                {
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@item.Id</td>
                                            <td>@item.Name</td>
                                            <td>@GetEnumDescription.Description(item.Status)</td>
                                            <td><a href="/Admin/Brand/UpdateBrand/@item.Id" class="btn btn-outline-success">Güncelle</a></td>
                                            <td><a href="/Admin/Brand/BrandDetails/@item.Id" class="btn btn-outline-primary">Detaylar</a></td>
                                            <td><a href="/Admin/Brand/SoftDelete/@item.Id" class="btn btn-outline-danger">Sil</a></td>
                                        </tr>
                                    }

                                }

                            </tbody>
                        </table>
                        <a href="/Admin/Brand/CreateBrand" class="btn btn-outline-primary">Yeni Marka Girişi </a>
                        <a style="margin-left:10px" href="/Admin/Brand/DeletedBrandList" class="btn btn-outline-danger">Silinen Markalar</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>